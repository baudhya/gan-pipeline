epochs: 200
batch_size: 1
lr_generator: 0.0002
lr_discriminator: 0.0002
beta1: 0.5
beta2: 0.999
loss_type: hinge
lambda_l1: 100.0
lambda_vgg: 10.0
vgg_weights_path: weights/vgg16-397923af.pth   # run `make download-weights` once to populate; set to null to download on first use instead
lambda_fm: 10.0
save_every: 10
sample_every: 5
log_every: 100
num_workers: 4
