epochs: 200
batch_size: 1
lr_generator: 0.0002
lr_discriminator: 0.0002
beta1: 0.5
beta2: 0.999
loss_type: hinge
lambda_l1: 100.0
lambda_vgg: 10.0
vgg_weights_path: null   # null = download on first use; for offline use: run `make download-weights`, then set to weights/vgg16-397923af.pth
lambda_fm: 10.0
save_every: 10
sample_every: 5
log_every: 100
num_workers: 4
